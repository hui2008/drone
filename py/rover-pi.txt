device-tree directory not found 
Usage: --serial0 serial0Path --serial1 serial1Path 
Examples:
	serial (0 through 9 available):
	                  --serial0 /dev/ttyO4
	                  --serial3 /dev/ttyS1
	legacy UART options are deprecated, their mappings are:
	                  -A/--uartA is SERIAL0
	                  -C/--uartC is SERIAL1
	                  -D/--uartD is SERIAL2
	                  -B/--uartB is SERIAL3
	                  -E/--uartE is SERIAL4
	                  -F/--uartF is SERIAL5
	                  -G/--uartG is SERIAL6
	                  -H/--uartH is SERIAL7
	                  -I/--uartI is SERIAL8
	                  -J/--uartJ is SERIAL9
	networking tcp:
	                  --serial1 tcp:192.168.2.15:1243:wait
	                  --serial0 tcp:11.0.0.2:5678
	                  --serial0 udp:11.0.0.2:14550
	                  --serial0 udp:11.0.0.2:14550
	networking UDP:
	                  --serial0 udp:11.0.0.255:14550:bcast
	                  --serial0 udpin:0.0.0.0:14550
	custom log path:
	                  --log-directory /var/APM/logs
	                  -l /var/APM/logs
	custom terrain path:
	                   --terrain-directory /var/APM/terrain
	                   -t /var/APM/terrain
	custom storage path:
	                   --storage-directory /var/APM/storage
	                   -s /var/APM/storage
	module support:
	                   --module-directory /usr/lib/ardupilot/modules
	                   -M /usr/lib/ardupilot/modules
	cpu affinity:
	                   --cpu-affinity 1 (single cpu) or 1,3 (multiple cpus) or 1-3 (range of cpus)
	                   -c 1 (single cpu) or 1,3 (multiple cpus) or 1-3 (range of cpus)
